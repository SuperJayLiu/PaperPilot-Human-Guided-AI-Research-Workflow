# AI Conversations

Save full prompts and responses here. The first real project prompt should be:

```text
000_initial_prompt.md
```

Use one file per substantial agent session. Include model/tool name, date, prompt, response, files changed, and whether the output was accepted.
